<% provide(:visualization, "active") %>

<%= render 'title' %>

<section>
    <div class="container">
      <div class="animated fadeIn">
        
        <%= render 'top' %>

            <div class="row">
                <div role="tabpanel">
                
                    <%= render 'tabs' %>
                    
                    <!-- Tab panes -->
                    <div class="tab-content">
                    
                        <div role="tabpanel" class="tab-pane active" >
                            <i class="fa fa-picture-o pull-left fa-4x"></i>
                            <p>Visualization Services</p>
                            <p>We offer Photorealistic product visualization and Product animations</p>
                        
                    </div>
                
                </div>
            </div>

        <%= render 'vis_images' %>
    </div>
  </div>
</section>